{
  "buildCommand": "DATABASE_URL=file:./build.db prisma generate && DATABASE_URL=file:./build.db prisma db push --accept-data-loss && DATABASE_URL=file:./build.db node prisma/seed.js && next build",
  "framework": "nextjs",
  "env": {
    "DATABASE_URL": "file:/tmp/dev.db"
  }
}
